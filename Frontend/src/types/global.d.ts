// Global type extensions
interface Window {
  // Add styled-components for iframe sandbox rendering
  styled: object;
  // Add React for iframe sandbox rendering
  React: object;
  // Add ReactDOM for iframe sandbox rendering
  ReactDOM: object;
} 