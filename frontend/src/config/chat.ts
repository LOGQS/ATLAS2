// status: complete

export const MAX_CONCURRENT_STREAMS = Number(
  process.env.REACT_APP_MAX_CONCURRENT_STREAMS || 3
);

export const DEBUG_TOOLS_CONFIG = {
  showTriggerLog: false,

  showPerformanceMonitor: false
};

